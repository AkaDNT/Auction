services:
  redis:
    image: redis:7
    container_name: auction_redis
    ports:
      - "${REDIS_PORT:-6379}:6379"
    volumes:
      - auction_redis:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 20

volumes:
  auction_redis:
